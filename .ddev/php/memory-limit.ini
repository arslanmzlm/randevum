; DDEV ships memory_limit=-1. An unbounded limit turns a runaway allocation (a circular
; container binding, a query loading a whole table) into a host-level OOM instead of a PHP
; fatal error: the process keeps growing until the kernel kills something, and on a dev
; machine that something is the desktop session. A finite limit keeps the failure inside PHP.
memory_limit = 1G
