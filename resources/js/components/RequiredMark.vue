<script setup lang="ts">
// Shared required-field asterisk, so a manual (non-FormField) label matches FormField's marker
// instead of re-typing the markup per call site.
</script>

<template>
    <span class="text-red-500"> *</span>
</template>
